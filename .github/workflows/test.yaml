name: Hello 

on: push 

jobs:
  build: 
   runs-on: ubuntu-latest
   
   steps:
     - name: Run
       run: |
        echo  secret: ${{  secrets.HELLO_SECRET }} >> .env 
     - name: cat env 
       run: | 
        cat .env 
        ls -lha 

        
        
  test:
    runs-on: ubuntu-latest 
    steps: 
      - name: hacker 
        run: echo "hello hocker" 
